{
  "name": "xbigbrainnx-portfolio",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-config.js",
    "dev": "npm run build && python3 -m http.server 8080"
  },
  "devDependencies": {
    "dotenv": "^16.4.7"
  }
}
